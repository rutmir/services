# services

Required envirinment parameters

for chat-rest:
LOG_PATH_SEPARATOR = /services

for chat-endpoint & chat-worker 
LOG_PATH_SEPARATOR = /services
AMQP_HOST =
AMQP_PORT =
AMQP_USERNAME =
AMQP_PASS =
