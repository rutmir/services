# services

Required envirinment parameters

for chat-rest:</br>
LOG_PATH_SEPARATOR = /services

for chat-endpoint & chat-worker:</br>
LOG_PATH_SEPARATOR = /services
AMQP_HOST =
AMQP_PORT =
AMQP_USERNAME =
AMQP_PASS =
